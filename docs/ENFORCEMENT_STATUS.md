# Enforcement Status

**Last verified:** 2026-06-22 — Series Planner four-movement re-leveling: `melodic_evidence` added to `SERIES_COLUMNS` across all three mirrors (contracts.ts / contracts.cjs / test-spine — allowlist-sync test still gates); schema v26 (additive column + run-once `book_structure`→`structural_outline` fold, `book_structure` retained as backup). Additive, no clause weakened; create-then-update preserved (INSERT not widened).
**Verified against:** [`docs/CORE.md`](CORE.md).
**History:** see `git log docs/ENFORCEMENT_STATUS.md` and [`CHANGELOG.md`](../CHANGELOG.md). Per-event narrative is intentionally not duplicated here.

## Current state

- Audit triage initiative closed. All five primary pilots shipped (C / D / E / B.2 / B.3 — Surface Contract + State #6).
- All four post-pilot deferred-bucket items shipped: `react/jsx-no-undef` + `no-undef` enabled; view-key + workspace tab-key vocabulary completion via `VIEW.*` and `STAGE.*` in [`src/core/contracts.ts`](../src/core/contracts.ts); CTA primitive set complete with `<TextButton>`.
- `canonical-stage-name` / Surface #1 / State #5 lint forbidden set: `writing`, `ready`, `archived`, `planning`, `active`, `study`, `outline`. CSS-class-context exemption applies under `className`.
- Four CTA primitives (`PrimaryButton`, `SecondaryButton`, `IconButton`, `TextButton`) cover every Surface #2 CTA shape; `no-raw-button` is at **zero** across the codebase (the final D2c/D2d writing-surface hits migrated to `<IconButton>` on 2026-06-21).
- Process Contract #6 active as Structural; the canonical articulation lives in [`docs/WORKSPACE-CANON.md`](WORKSPACE-CANON.md) (ratified 2026-06-15) and the throughline's structural integrity is testable against the canon. The SFDI + SADI working docs are the frozen, banner-stamped development records.
- Process Contract #4 lead clause: *"Pastoral Context is driven by the text, not the other way around"* (2026-05-01 wording sharpening only; mechanism unchanged).

## Summary

| Layer | Count | Notes |
|---|---|---|
| Structural | 17 clauses | Impossible to violate by API shape (compile or runtime). |
| Test | 2 clauses | Caught at CI by failing tests; no structural impossibility, but test gates merge. |
| Lint | 3 clauses | Caught at editor and pre-commit by custom ESLint rules. |
| Fully unenforced | 0 clauses | Every active clause in `docs/CORE.md` has a real primary enforcement mechanism. |
| Sub-clause portions named-deferred to a specific successor | 5 portions | 3 to Phase 4 component primitives (Mutation #2 ProposalPanel generalization, Mutation #4 DestructiveAction, Mutation #5 ErrorBanner / InlineError); 1 to Phase 6 save+error pipeline (Mutation #3 SaveIndicator); 1 to SPRD (Process #2 per-stage evidence-sufficiency). (Process #4 phase mechanics is no longer deferred — moved to the `sermon-workspace.md` spec in Re-Foundation examination 2, 2026-06-14.) The clauses themselves are enforced at primary; the deferred sub-portions are clearly-owned successor work, not gaps. |

A single clause may be enforced at multiple layers — the count above assigns each to its **primary** mechanism. Secondary layers are listed in the per-clause table.

## Per-clause status

| Clause | Layer | File / mechanism | Verification command |
|---|---|---|---|
| State #1 — series + sermon are first-class state | Structural | `src/core/spine.ts` is the only sermon/series API; integrity gate at `scripts/spine-integrity.js` blocks all bypasses | `node scripts/spine-integrity.js` |
| State #2 — every sermon has a canonical position | Structural | v17 migration added `current_stage / current_step / current_sub_phase` columns; `spine.getSermon()` derives `position: ProcessPosition` from them. v21 (2026-05-11) added `last_study_subphase / last_assembly_subphase` so per-stage memory lives in the canonical record instead of localStorage; `transitionState` writes them, renderer reads them. v23 (2026-05-17, trail deletion sweep Phase D1) added `last_touched_position` for field-granular session re-entry and `thresholds_seen` for threshold dismissal; both are renderer-written via `persistUpdate` and intentionally NOT in `SPINE_ONLY_COLUMNS`. `current_step` retired in the same sweep (Phase B2) — canonical position is now `(current_stage, current_sub_phase)` only; field-level position carried by `last_touched_position`. `SPINE_ONLY_COLUMNS` (in `contracts.ts` + `contracts.cjs`) keeps user-edit saves from clobbering spine-written columns. | `npm test -- tests/contracts/process-1-monotonic.test.ts` |
| State #3 — no anonymous atoms | Structural (renderer fast-fail + IPC re-validation) | Renderer: `src/core/spine.ts` `createSermon` / `createSeries` throw `ContractViolation`. Main: `validateAndCommit` re-rejects with `code: STATE_3_NAMELESS_*` | `npm test -- tests/contracts/state-3-no-anonymous-atoms.test.ts` |
| State #4 — parent context first-class | Structural | `spine.getSermon()` computes `parentContext: { seriesId, seriesName, positionInSeries, totalInSeries }` from sibling order at read time | `node scripts/spine-integrity.js` (the spine is the only path) |
| State #5 — one name per concept | Lint + Test | Forbidden-alias set (post-vocabulary-completion): `writing`, `ready`, `archived`, `planning`, `active`, `study`, `outline`. Lint: `sermonforge/canonical-stage-name` with CSS-class-context exemption (literals nested under a `className` JSX attribute aren't flagged — those are CSS class names, not state/status/route identifiers). Test: `tests/contracts/state-5-one-name-per-concept.test.ts`. | `npm run lint && npm test -- tests/contracts/state-5-one-name-per-concept.test.ts` |
| State #6 — in-progress queryable from front door | Structural | `spine.getInProgressSermons()` is wired into the Dashboard Resume Work tile, which renders top in-progress sermons with a return-day reminder section for sermons whose delivery date has passed but stage is still `in_progress`. Mark Complete on the Delivery tab + Mark Series Complete in the SeriesPlanner topbar provide the lifecycle close-out, with auto-suggest banners that surface the action without performing it (Principle: system suggests, user decides). | `node scripts/spine-integrity.js` (API surface present + consumed) |
| Process #1 — movement is monotonic in expectation, not enforcement | Structural (by absence of enforcement, by presence of map) + Lint | Contract rearticulated 2026-05-18 (Trail deletion sweep Phase G) from "movement is monotonic by default; forward through stages is the natural direction; backward movement is allowed but explicit" to "movement is monotonic in expectation, not enforcement" — the spine-side forward-to-prior rejection (`PROCESS_1_FORWARD_TO_PRIOR` in `electron/main.js transitionState`) was deleted in G, completing what F started on the renderer side (the `evaluateAdvance` wall layer). Today's enforcement is by-absence: nothing refuses navigation. The expectation lives in the writing surface's flow (chevron-next moves forward through `walkOrder.nextField()`) and in the map's vertical question list, which shows the full arc on first entry with state by visual weight. The pre-G `tests/contracts/process-1-monotonic.test.ts` was deleted alongside the rejection it tested. **No test today; the absence of the rejection is the enforcement.** A new test could assert "no spine handler emits `PROCESS_1_FORWARD_TO_PRIOR`" as a tripwire, but that pattern hasn't been wired yet — opportunistic future work. | (No dedicated test post-G — the rejection's absence is the structural artifact; spine-integrity scan covers the broader surface) |
| Process #2 — a sermon is complete when its load-bearing artifacts exist | Structural (composites) + partial-surface | Contract rearticulated 2026-05-18 (Trail deletion sweep Phase G) from "movement is gated by user evidence; the constraint *is* the gate" to "a sermon is complete when its load-bearing artifacts exist" — the advancement-wall the original clause described was deleted across F (renderer-side `evaluateAdvance` + wrappers + evidence builders + formatters) and G (spine-side `PROCESS_2_EMPTY_EVIDENCE` rejection in `electron/main.js transitionState`, plus the `isLegacySermon` + `getLegacyEvidenceCutoff` carve-out machinery). The deeper commitment — sermon completeness depends on load-bearing artifacts — survives in the **8 composite gate functions** in `src/utils/studyAdvancement.js` (`checkField3Composite`, `checkField8Composite`, `checkPhase4Field4Composite`, `checkField5Composite`, `checkIntroComposite`, `checkConclusionComposite`, `checkMPTComposite`, `checkMPSComposite`). The map-weight derivation in `src/utils/sermonState.js` (`deriveQuestionStatesFromSermon`, `deriveStudyOutcomesFromSermon`, `deriveStudyUnfinishedFromSermon`) consumes the same per-question-kind dispatch principle and powers the Study → Anchor handoff's active surfacing of missing required outcomes. **The composites are wired** (2026-06-10) into `deriveSermonCompleteness` in `src/utils/sermonState.js`, which `SermonWorkspace.jsx` + `SermonFinish.jsx` consume as the workspace-wide "is the sermon done" answer — informational at the Finish screen with per-artifact "go write it" jumps, never blocking (Process #1). The contract is real and now surfaced. (Corrected 2026-06-15, Re-Foundation step 4: this row previously said the composites had no callers / surfacing was in progress — stale; the code at HEAD wires them.) The pre-G `tests/contracts/process-2-evidence-gated.test.ts` was deleted alongside the rejection it tested. | (No dedicated test post-G — composite-level coverage rides on the surfaces that consume them; Study → Anchor handoff covered by `process-3-movement-visible.test.tsx`) |
| Process #3 — movement is visible at thresholds, not narrated continuously | Structural + Test | Contract rearticulated 2026-05-17 (Trail deletion sweep Phase D2e) from "movement is a visible event; 'Continue' is movement, and movement is never silent" to "movement is visible at thresholds, not narrated continuously" — citing the invisible-system build spec's *Strategic orientation at thresholds* line and the era-2 primacy charter's *Constraint without ceremony* clause. Surface shipped at D2c: `SermonWorkspace.jsx` no longer renders the tab-change `movement-event` banner. Discrete threshold surfaces — `SermonStartLanding` (`.ssl-overlay`) on first session, `StudyAnchorHandoff` (`.sah-overlay`) at Study→Anchor — carry the visibility. Within-stage chevron-next + map-jump are silent per the calm-surface principle. **Test rewrite shipped at D2e:** `tests/contracts/process-3-movement-visible.test.tsx` asserts the new vocabulary (sermon-start fires `.ssl-overlay` on null `last_touched_position`; Study→Anchor crossing fires `.sah-overlay`; within-stage movement produces no overlay and no `data-testid="movement-event"`). Meta-test inverted as the no-narration tripwire: no component under `src/components/` may carry `data-testid="movement-event"`. | `npm test -- tests/contracts/process-3-movement-visible.test.tsx` |
| Process #4 — PC follows the text | Test (shell) | RTL test asserts brand-new sermon with empty PC + observations renders at Observe without gating. **Phase mechanics specified in `docs/SYSTEMS/sermon-workspace.md`: PC enters at Implications (the three-voice conversation), not progressively from Observe** (the Observe "Possible Implications" surface was removed 2026-06-15 in the Phase-2 Merida surgery, and CORE Process #4 was amended to match; moved out of CORE to the spec in Re-Foundation Phase 1 examination 2) | `npm test -- tests/contracts/process-4-pc-follows-text.test.tsx` |
| Process #5 — No AI substitution | Structural (by absence) + Lint | ARI Phase 9 (2026-05-09) rewrote the clause to "The system contains no AI authorship surfaces. The pastor authors all sermon content." Enforcement is now structural-by-absence: the AI subsystem (electron/ai.js, provider.js, sendAIMessage, `@anthropic-ai/sdk`, prompts, context pipeline) was deleted — file names are listed plain (no backticks) because the files no longer exist; this is historical context, not a live reference. ESLint `sermonforge/no-direct-ai` is the lint-layer tripwire (broadened 2026-06-21): any `@anthropic-ai/*` import/require, any `.sendAIMessage(...)` call on any object, and any reopening of the removed `ai-message` IPC channel (`ipcRenderer.invoke` / `ipcMain.handle`) are rejected, no exceptions. | `npm run lint` (zero violations; tripwire fires on any reintroduction) |
| Process #6 — workspace throughline is structural | Structural (SFDI + SADI scaffolding) | Clause renamed from "Study throughline" → "workspace throughline" 2026-05-04 per SADI Ruling 4; outcome-scope widened to "Each workspace step (and each Study sub-phase) produces a named outcome"; canonical articulation now lives in `docs/WORKSPACE-CANON.md` (ratified 2026-06-15, Re-Foundation step 5; the throughline's structural integrity is testable against the canon). The SFDI working doc (`docs/PROPOSALS/study-field-definition-initiative.md` — Study: 25 field entries with seven slots, 4 named outcomes, 4 sub-phase boundary handoffs) and SADI working doc (`docs/PROPOSALS/sermon-anchor-definition-initiative.md` — Anchor/Frame: 4 anchor fields, 2 named outcomes, handoffs) are now frozen, banner-stamped development records. Two validator scripts parse SFDI for structural integrity: `scripts/sfdi-internal-consistency.py` and `scripts/sfdi-cross-doc-consistency.py` (cross-doc trimmed 2026-05-06 — C2 dropped SPRD from its doc list, C3 dropped its SPRD-specific 8+8+5+4 check, C4 and C6 retired entirely as brittle drift sources tied to a 579-line SPRD planning doc that was trimmed to 110 lines on 2026-05-05). SADI scaffolding currently rests on its own working-doc structural integrity; sibling SADI validator scripts deferred per SADI charter until per-field content accumulates enough surface to test against. Throughline's substantive integrity binds the writer across both docs; the scripts are the mechanical evidence for SFDI side. | `python scripts/sfdi-internal-consistency.py && python scripts/sfdi-cross-doc-consistency.py` |
| Mutation #1 — user typing wins | Structural (by absence of system actors) | ARI Phase 9 (2026-05-09) rewrote the clause: "All sermon content is pastor-typed. There are no system-driven writes to sermon fields outside the pastor's keystrokes." The pre-ARI proposal/apply cycle that gated AI writes was removed. `MutationKind` collapsed to `user_input` only; `validateAndCommit` accepts only that kind. Existing test (`mutation-1-user-typing-wins.test.ts`) still passes against the simplified one-kind path. | `npm test -- tests/contracts/mutation-1-user-typing-wins.test.ts` |
| Mutation #2 — Retired (ARI Phase 9, 2026-05-09) | n/a | The original clause reserved a separate slot for AI proposals so the pastor's existing field was never touched while a proposal was pending. With AI removed, no system actor ever writes to a sermon field; the slot mechanism is unnecessary. `ProposalPanel.jsx` deleted. | n/a |
| Mutation #3 — saves are events | Structural + Test | `spine.persistMutation(setSaveState, doMutation)` is the canonical save-state helper; `SermonWorkspace.jsx` consumes it. **`<SaveIndicator>` primitive deferred to Phase 6** | `npm test -- tests/contracts/mutation-3-saves-are-events.test.ts` |
| Mutation #4 — destruction proportional to reversal | Structural (Component) | `src/components/DeleteButton.jsx` is the canonical two-step inline confirm; used everywhere destructive UI lives. **`<DestructiveAction>` primitive generalization deferred to Phase 4** | (No dedicated test in Phase 5 — DeleteButton's contract is the structural artifact) |
| Mutation #5 — errors speak in one voice | Lint | `sermonforge/no-window-alert` flags `alert()` / `confirm()` / `window.alert()` / `window.confirm()`. Component pattern: `<InlineError>` for field-level, App.jsx banner for retryable. **`<ErrorBanner>` primitive deferred to Phase 4** | `npm run lint` (zero violations today) |
| Surface #1 — one vocabulary | Lint + Test | Lint: `sermonforge/canonical-stage-name` (narrowed forbidden set). Test: `tests/contracts/surface-1-one-vocabulary.test.ts` shares scan helper with State #5 | `npm run lint && npm test -- tests/contracts/surface-1-one-vocabulary.test.ts` |
| Surface #2 — one CTA system | Structural + Lint | The four canonical CTA primitives — `src/components/primitives/{PrimaryButton,SecondaryButton,IconButton,TextButton}.tsx` — cover every CTA shape (primary, secondary, icon-only, tertiary text-link). `sermonforge/no-raw-button` flags every `<button>` outside `src/components/primitives/` to keep new code on the primitives. **Lint-baseline reset post-trail-deletion (Phase E, 2026-05-17):** the pre-E baseline of 5 residual tab/pill nav hits (`stage-tab`, `step-pill`, `subphase-pill`, `delivery-tab-btn`, planner top-tabs) was emptied by Phase E (the files that carried those buttons — `StudyTab`, `AssemblyTab`, `ManuscriptTab` — were deleted). The new baseline is **23 raw-button hits across the post-D2c writing-surface stack**: `SermonWritingSurface.jsx`, `SermonMap.jsx`, `SermonStartLanding.jsx`, `StudyAnchorHandoff.jsx`, `WorkspaceNotebookDrawer.jsx`, `SynthesisTable.jsx`, plus pre-existing hits in `FieldOverviewScreen.jsx`, `IndentedSentenceCanvas.jsx`, `ScripturePanel.jsx`. These are CTAs (Close, Continue, dismiss, jump-to-field, threshold actions) — within Surface #2's scope — and represent a real catch-up pass: D2c/D2d shipped new surfaces inline without routing through the primitives. **Surface #2 lint catch-up is now scheduled work**, not contract-driven sequel hygiene. | `npm run lint` (23 `no-raw-button` hits; see Lint baseline table below for source distribution) |
| Surface #3 — one empty-state + loading vocabulary | Structural + Lint | `src/components/primitives/{LoadingState,EmptyState}.tsx` are the canonical loading + empty-state shapes. `LoadingState`'s `verb` prop is typed against the `LoadingVerb` union in `src/core/contracts.ts` (`"Loading…"` / `"Saving…"` / `"Thinking…"`). `<PrimaryButton loading={...}>` auto-renders the canonical verb. `sermonforge/canonical-loading-verb` zero-violations on committed code; rule now exempts JSX attribute values so placeholders aren't false positives. | `npm run lint` (zero `canonical-loading-verb` errors today) |
| Surface #4 — "you are here" is always answerable | Test | `tests/contracts/surface-4-you-are-here.test.ts` parses router destinations from `App.jsx` and sidebar entries from `Sidebar.jsx`, asserts router ⊆ sidebar ∪ EXPECTED_DEEP. Pilot B.2 closed the `archive` exception by renaming the route to `CompletedSermons` and adding the canonical sidebar entry. Post-revival (series-planner-revival, 2026-06-21), `Planning` is a canonical sidebar destination ("Series Planning" in `Sidebar` NAV_ITEMS, `VIEW.Planning`) and `SeriesPlanner` is a deep route reached via `openPlanner(id)`; both render live components. `EXPECTED_DEEP` still lists `SeriesPlanner` + `Workspace` (and, harmlessly, `Planning`); Workspace is reached by clicking a sermon card. | `npm test -- tests/contracts/surface-4-you-are-here.test.ts` |
| Surface #5 — one re-entry convention | Structural | `src/components/primitives/BackButton.tsx` is the canonical back-affordance shape; supplies the `←` prefix automatically. Two variants: `labeled` (default, "← Back" or custom child text like "Return to Study") and `icon` (chevron only). Four back-shape sites migrated: SermonWorkspace topbar chevron, SermonWorkspace error case, SeriesPlanner topbar, OutlineTab "Return to Study". | (No dedicated test — the primitive's existence + consumer adoption is the structural artifact; surfaces with no back affordance at all remain a UX-design decision, not a contract gap) |

## The Spine Integrity Gate

**Status:** active
**Location:** `scripts/spine-integrity.js`
**Wired into:** `.husky/pre-commit` (also runs `lint-staged`)
**Last failure-injection test:** 2026-04-30, result: pass (gate fires on injected `db.run(...)` in `src/components/Calendar.jsx`, restores cleanly)

The gate scans the codebase on every commit and CI run for four bypass classes:

1. `db.run` / `db.prepare` / `db.exec` outside `electron/main.js`
2. Raw `INSERT` / `UPDATE` / `DELETE` SQL on `sermons` / `series` / `series_sections` outside `electron/main.js`
3. `window.electronAPI.spine(...)` outside `src/core/spine.ts` (+ `electron/preload.js` which exposes the bridge)
4. Imports of any spine-only function name from `src/db/database.js` (with or without `.js` suffix) outside `src/core/`

**If this gate fails, every structural enforcement claim above is invalidated. Do not commit. Do not ship.**

## Deferred enforcement (known gaps with owners)

### Phase 4 — Component primitive layer (lands during audit triage)

- **Mutation #2** surface side: clause retired by ARI Phase 9 (2026-05-09). The proposal slot mechanism is unnecessary now that no system actor writes to sermon fields. `ProposalPanel.jsx` deleted.
- **Mutation #4**: `<DestructiveAction>` primitive enforces "destruction proportional to reversal cost" beyond the existing `<DeleteButton>`. (Pilot C moved `DeleteButton` from `src/components/` to `src/components/primitives/` so its three internal raw `<button>`s are exempt from `no-raw-button`; the canonical-pattern role is unchanged.)
- **Mutation #5** surface side: `<ErrorBanner>` and `<InlineError>` primitives complete the lint-only enforcement currently in place.
- **~~Surface #2~~** (Pilot C + TextButton follow-up + D2c/D2d catch-up — landed): `<PrimaryButton>` / `<SecondaryButton>` / `<IconButton>` / `<TextButton>` primitives in `src/components/primitives/`. All raw `<button>` violations migrated; `no-raw-button` is at zero (the final 25 D2c/D2d writing-surface hits routed through `<IconButton>` on 2026-06-21).
- **Surface #3** (Pilot D — landed): `<EmptyState>` / `<LoadingState>` primitives in `src/components/primitives/`. 30 non-canonical loading verbs replaced (Drafting/Generating/Reviewing/Synthesizing/Compiling/Assisting/Analyzing/Running → Thinking; Submitting/Creating/Exporting/Retrying/Formatting → Saving; Fetching scripture → Loading). Three empty states migrated to `<EmptyState>` (Planning, Archive, SermonList) as the pattern demo; further empty-state surfaces opt in incrementally.

### Phase 6 — Save and error pipeline integration

- **Mutation #3** surface integration: `<SaveIndicator>` primitive renders the structured `{ ok, code?, clause?, message? }` envelope from `persistMutation`. Currently structural at the spine layer; surface rendering pattern lives ad-hoc in `SermonWorkspace.jsx`.

### Specific audit-triage pilot dependencies

- **~~Pilot B.2 (Archive → Completed Sermons)~~** — landed. Route renamed `archive` → `completed-sermons`; `tests/contracts/surface-4-you-are-here.test.ts` `EXPECTED_DEEP` no longer contains the route. Per-sermon re-export wired by reusing the existing `sermon-export-manuscript` IPC (no new IPC channel needed).
- **~~Pilot B.3 (Dashboard "Resume your work")~~** — landed. Resume Work tile + return-day reminder + Mark Complete on Delivery + Mark Series Complete in planner all wired. State #6 surface gap closed; auto-suggest banners surface actions without performing them so the Principle (Clarity through Constraint) is preserved.
- **~~Workspace tab-key PascalCase migration~~** — landed. `TABS = STAGE_SEQUENCE` from `src/core/contracts.ts`; activeTab values are `STAGE.{Study,Blueprint,Manuscript,Delivery}`; `contextBuilder.js` switch cases, `reviewPrompts.js`, `prompts/sermon.js`, `memory.js` capture set, and `workspaceTourStops.js` all updated in lockstep. localStorage migration in `SermonWorkspace.jsx` maps pre-migration lowercase values to the canonical Stage so existing sermons restore the correct tab on next mount. `study` and `outline` are now in the `canonical-stage-name` forbidden set.
- **~~View-key migration~~** — landed. Top-level routes consume `VIEW.{Dashboard,Sermons,Calendar,CompletedSermons,Planning,SeriesPlanner,Workspace}` from `src/core/contracts.ts`; App.jsx + Sidebar.jsx migrated; the Surface #4 test parser was extended to also accept `VIEW.<Name>` enum references alongside literal strings. `planning` and `active` are now in the `canonical-stage-name` forbidden set; the rule's CSS-class-context exemption keeps `nav-item.active` and similar legitimate CSS modifier usages clean.
- **~~Pilot E (re-entry convention)~~** — landed. `<BackButton>` primitive lands in `src/components/primitives/`; four back-affordance sites migrated. Surface Contract #5 closed.

### SFDI (Study Field Definition Initiative)

**Status as of 2026-05-04:** SFDI's four-sub-phase walk is COMPLETE. The frozen SFDI working doc at `docs/PROPOSALS/study-field-definition-initiative.md` carries 25 field entries, 4 named outcomes (Observation Set, Interpretation Set, Christ-Connection Statement, Implications Synthesis), and 4 sub-phase boundary handoffs (including out of Study into MPT/MPS). Process Contract #6 activated. (The **live** Study field count is now **23** — `applications` and `genre` were removed 2026-06-15 in the Phase-2 Merida surgery; the canon, not the frozen SFDI doc, is the live source.)

- **Process Contract #6 — Study throughline is structural:** clause introduced 2026-04-30 alongside the SFDI charter; activated 2026-05-04 when SFDI's four-sub-phase walk completed. Binding: the throughline's integrity (every field contributes, every named outcome is built from the field-work, every handoff is explicit). Pedagogical: number, wording, exact text of fields and named outcomes — may evolve. Two validator scripts parse SFDI for visible scaffolding: `scripts/sfdi-internal-consistency.py` (7 criteria for internal structure) and `scripts/sfdi-cross-doc-consistency.py` (6 criteria for cross-doc consistency post-2026-05-06 trim — was 7). The throughline's substantive integrity — does each field actually contribute, does each named outcome follow from the field-work, does the handoff actually carry — binds the writer; the scripts are the mechanical evidence. **Scope extension ratified by SADI 2026-05-04:** Process #6 extends to all workspace steps through Delivery (no separate Process #7 was created). The throughline runs from Phase 1 Field 1 (Context) all the way to Manuscript/Delivery; cutting it across two contracts would invent a seam where the pastor experiences none. The CORE.md text edit consummating this extension ("Study throughline" → "workspace throughline" + canonical-articulation pointer to SADI alongside SFDI) lands as a small downstream code-edit pass.

### SADI (Sermon Anchor Definition Initiative)

**Status as of 2026-05-04 evening:** SADI's structural ratification walk + all four per-field content-design walks COMPLETE. Working doc at `docs/PROPOSALS/sermon-anchor-definition-initiative.md` carries seven-slot entries for all four anchor fields (MPT, MPS, Intro, Conclusion), two named-outcome declarations (Main Point Pair for Step 2; Sermon Frame for Step 5), step-boundary handoff articulations, a SADI-wide commitments section consolidating the cross-field rulings (cumulative-table closure at 6 columns, Process #6 extension, N/A scope, AI principle), AND pastor-side content-design copy per field (overview blockquotes, Q-framings, Eph 2:1–5 worked example outputs in pastor-to-people voice). Voice principle locked: pastor-to-people ("we / us / you") for all framing + examples, never pastor-describing-the-sermon. No SADI internal-name references in pastor-facing framing copy.

**Downstream consumers shipped 2026-05-04:**
- **SPRD C3 (Step 5 elevation) SHIPPED** — STAGE.Frame between Blueprint and Manuscript; v18 migration adds `sermon_frame` JSON column; new SERMON_FRAME_FIELDS in `src/utils/sermonFrameFields.js` consumes SADI's Intro + Conclusion field defs verbatim (overview body text + question prompts); new FrameTab + composite gate at Frame → Manuscript; 15 new contract tests. State #2 (canonical position) Stage union extended to 5; sweep-the-house PASS.
- **~~MPS_DRAFT prompt rewrite~~** — shipped 2026-05-04 as three per-question prompts in the now-deleted src/prompts/study.js (file no longer exists; backticks dropped so drift-check doesn't flag a historical name). **Retired by ARI Phase 8 (2026-05-09):** the entire `src/prompts/` directory was removed alongside the AI subsystem. The MPS Q1/Q2/Q3 field def in SADI is now consumed directly by the writing surface's question flow (the trail that previously rendered it was retired in Phase E, 2026-05-17) — no AI prompt reads from it.
- **CORE.md Process #6 text extension SHIPPED** per SADI Ruling 4 — see Process #6 row in per-clause table above for the full new text.

- **Process Contract #6 — extension to all workspace steps:** ratified by SADI 2026-05-04 (see SFDI entry above for the full extension note). The contract's structural commitments (every field contributes, every named outcome is built from field-work, every handoff is explicit) now apply across SFDI's four sub-phases AND SADI's two anchor steps; future Steps 3 (Outline) and 4 (Functional Elements) initiatives, when walked, fall under the same contract.
- **~~AI principle (SADI-wide)~~:** retired by ARI Phase 9 (2026-05-09). Pre-ARI the principle was "AI clarifies the pastor's voice; it does not author it" with AI Draft / AI Suggest / AI Tighten affordances at the SADI layer reading the rough substrate. With AI removed, the pastor authors every word; the SADI question flow itself is the structural surface that asks-without-substituting. Process Contract #5 ("No AI substitution") now carries this commitment structurally-by-absence.
- **Validator script extension** (deferred): existing SFDI validator scripts may extend to parse SADI scaffolding too, or SADI may get its own validator scripts modeled on SFDI's. Decision deferred until per-field content-design walks accumulate enough additional content to warrant dedicated parsing.

### SPRD (Study Phase Re-Design)

**Status as of 2026-05-02:** SPRD is structural-only after the same-date content merge into SFDI. SPRD's content sections (named-outcomes substance, evidence-sufficiency thresholds substance, Q2/Q3b/Q6/Q7-content) all moved to SFDI; SPRD now owns the structural skeleton — spine routing, evidence-gate firing mechanics, hard-gate posture, Implications restructure shape, Step 5 as its own workspace step, PC card removal, Implications Synthesis named outcome, the Isolated-World Workspace UX overhaul, movement visibility. **Q1 spine routing, Q3 hard-gate UX, and Q8 inline AI Reviews disposition all landed 2026-05-02.** All SPRD open questions are now settled. **Structural backlog remains, sequenced behind SFDI:** Implications restructure (Q7 structural half), Step 5 (Intro/Conclusion) as its own workspace step, PC card removal, Implications Synthesis as the named outcome of the Implications sub-phase, and the **Isolated-World Workspace UX overhaul** (umbrella commitment with three components — field-level spotlight, sermon-level takeover, throughline visualization). The chain: SFDI ships Implications field content → Q7 content half lands → Q7 structural restructure ships → Step 5 + PC card removal follow; the Isolated-World Workspace UX overhaul ships with or after the workspace UX phase.

- **Process Contract #2 — completeness contract surfacing (wired):** the original advancement-wall (spine-side empty-evidence rejection + renderer-side disabled-Continue UX + `evaluateAdvance` + the seven `check*Threshold` wrappers) was deleted across F (2026-05-17, renderer side) and G (2026-05-18, spine side). The clause was rearticulated in CORE on 2026-05-18: completeness is the contract, not advancement. The 8 composite gate functions in `studyAdvancement.js` (`checkField3Composite` exported; 7 internal) are the foundation. **The workspace-wide "is the sermon done" answer is now wired (2026-06-10):** `deriveSermonCompleteness` in `sermonState.js` consumes all 8 composites (plus 3 deliberately lenient presence checks for Sermon Outline / Sermon Body / Manuscript), `SermonWorkspace.jsx` calls it for the Finish flow, and `SermonFinish.jsx` renders the artifact review (per-artifact "go write it" jumps, Export to Word, Mark-as-preached). The map-weight derivation in `sermonState.js` remains the continuous lower-weight surface (used by the Study → Anchor handoff). The answer informs, never blocks (Process #1). (Corrected 2026-06-15, Re-Foundation step 4: this bullet previously said the surfacing was "in progress / outstanding" — stale; HEAD wires it.)
- **Process Contract #4 — phase mechanics:** Pastoral Context's integration at Implications. The Phase 5 test asserts the structural shell ("PC absent ≠ Study locked"). Pre-ARI, the test also asserted a prompt-contract commitment that AI prompts treat PC as enrichment never as a precondition; that half is moot now (ARI Phase 8, 2026-05-09 — no AI prompts remain). **The PC mechanics are now specified in `docs/SYSTEMS/sermon-workspace.md`** — PC surfaces at a single point, Implications Field 3 Pastoral Context (with `room_specifics` and `cost_and_gift`); the product-owner's original progressive-PC articulation is retained there as dated historical rationale. CORE Process #4 keeps the *principle* (PC is driven by the text) as binding law and points to the spec for the mechanics. (Moved out of CORE to the spec in Re-Foundation Phase 1 examination 2, 2026-06-14; re-pointed 2026-06-15 from "lives in SFDI / SADI"; PC progression simplified to Implications-entry 2026-06-15 when the Observe "Possible Implications" surface was cut in the Phase-2 Merida surgery.)

## Test environment caveat

Contract tests use **Path B**: an in-memory fixture mirroring the spine boundary, not the real Electron main process. The fixture lives at `tests/contracts/_helpers/test-spine.ts` and reproduces `validateAndCommit` and `spineRead` from `electron/main.js`.

**Drift risk:** if `validateAndCommit` or `spineRead` change in `electron/main.js` and the fixture isn't updated in lockstep, contract tests will pass against stale logic.

**Mitigation today:** the fixture's clause citations (`clause: "State #3"`, etc.) and error codes (`code: PROCESS_2_EMPTY_EVIDENCE`, etc.) mirror the main process literally. Any divergence in citation strings will surface as a test failure when tests assert on `clause:` or `code:`. **2026-05-05 alignment pass:** scheduled weekly drift check identified five behavioral divergences (update-{sermon,series,section} fixture-only `NOT_FOUND` + `STATE_5_UNKNOWN_FIELD` rejections that main.js doesn't emit; missing `UPDATE_NO_FIELDS` rejection on the fixture side; missing `delete-series` + `delete-section` cascades) plus four `spineRead` sort divergences. Fixture rewritten to mirror main.js: shared `buildUpdate` helper, silent no-op on missing rows, full cascade in delete handlers, `created_at` tiebreakers + `year DESC, title ASC` + `COALESCE(updated_at, created_at) DESC` sorts, `section_title` join in `get-sermons-by-series`. Added `SERIES_COLUMNS` + `SECTION_COLUMNS` exports to mirror `src/core/contracts.ts`. 98 contract tests pass. **2026-06-21:** `tests/contracts/contracts-allowlist-sync.test.ts` now asserts the SERMON/SERIES/SECTION_COLUMNS allowlists are set-equal across `contracts.ts`, `contracts.cjs`, and this fixture — converting the column-mirror drift surface from manual review into a failing test. It immediately caught a stale fixture `SERMON_COLUMNS` (missing `last_study_subphase` / `last_assembly_subphase` / `notebook_*`), now re-synced.

**Mitigation candidate:** Path A (real Electron in tests via spectron / playwright / Electron-test-runner) would eliminate this drift surface. Out of scope for this enforcement pass; revisit if drift causes a regression. Documented in `tests/contracts/README.md`.

### Consumer-side import drift

**Status:** resolved (closed in the post-pilot deferred bucket).

The class of regression where a renderer component imports a name the spine doesn't export — for example, `getSeriesById` imported from `../core/spine` when the module only exports `getSeries` — used to pass every gate. The spine integrity gate scans for *forbidden* imports, not missing exports. `tsc` skips `.jsx` files by default. The Path B test fixture reproduces the spine API surface but doesn't exercise component imports. Pilot C silently introduced this exact bug in `SeriesPlanner.jsx` (primitives used without imports + `getSeriesById` rename missed) and it surfaced only as a runtime crash on the Series Planner page during the post-enforcement regression audit.

**Resolution:** `react/jsx-no-undef` and ESLint's built-in `no-undef` rule are now enabled at `error` in `.eslintrc.cjs`. The first catches missing-import cases on JSX components (`<Foo />` when `Foo` isn't in scope); the second catches the same on non-JSX symbols (function calls, member accesses, hook references). Both fire at editor time and at the lint-staged pre-commit hook. The Pilot C → SeriesPlanner regression class can no longer pass commit silently — `eslint-plugin-react` was already in `package.json` so the resolution was a `.eslintrc.cjs` config edit plus one fix (`BackButton.tsx` referenced `React.MouseEvent` without importing the namespace).

**Path A** (real Electron in tests) remains a candidate for a deeper class of integration bugs the lint rule can't catch (e.g., wrong-shape arguments to a real export). Still out of scope here; revisit if a regression of that class appears.

## Lint baseline accounting

| Rule | Current count | Resolved by |
|---|---|---|
| `sermonforge/no-raw-button` | 0 | **D2c/D2d catch-up pass landed 2026-06-21** — the remaining 25 hits across `IndentedSentenceCanvas.jsx`, `SermonMap.jsx`, `SermonWritingSurface.jsx`, `StudyAnchorHandoff.jsx`, `SynthesisTable.jsx`, `WorkspaceNotebookDrawer.jsx` all migrated to `<IconButton>` (the passthrough primitive — every site used a bespoke class, so the CTA primitives would have altered appearance; Pilot C is structural-only). Surface #2 is now genuinely at zero across the codebase. |
| `sermonforge/canonical-loading-verb` | 0 | Pilot D landed (36 → 0). Rule tightened to exempt JSX attributes; 30 non-canonical verbs replaced with the three canonical ones. **2026-06-21:** test exemption extended from `/tests/` dirs to the repo's colocated `*.test.{js,jsx,ts,tsx}` convention — a manuscript-opener fixture (`"Once..."`) was tripping the regex. |
| `sermonforge/no-direct-database` | 0 | — |
| `sermonforge/no-direct-ai` | 0 | Originally landed as ACCI D2 (2026-05-02) with an electron/ai/provider.js + src/utils/ai.js allowlist (file names listed plain — both deleted in ARI). Rewritten in the ARI hanging-chads pass (2026-05-09) as a no-exception tripwire — both allowlisted files were deleted, so any `@anthropic-ai/sdk` import or `window.electronAPI.sendAIMessage` call is now rejected. **Broadened 2026-06-21** (Series Planner audit remediation) to catch any `@anthropic-ai/*` subpath, `.sendAIMessage` on any object, and reopening the `ai-message` channel. Lint-layer enforcement of the no-AI invariant. |
| `sermonforge/canonical-stage-name` | 0 | Forbidden set expanded post-vocabulary-completion to `writing`, `ready`, `archived`, `planning`, `active`, `study`, `outline`. CSS-class-context exemption (literals nested under `className` JSX attributes) keeps the `.active` CSS modifier clean. |
| `sermonforge/no-window-alert` | 0 | — |
| `react-hooks/rules-of-hooks` | 0 | — |
| `react/jsx-no-undef` | 0 | Enabled post-audit-triage as the structural fix for the consumer-side import-drift class (see §"Consumer-side import drift" above). |
| `no-undef` | 0 | Enabled alongside `react/jsx-no-undef`; one pre-existing drift surfaced (`BackButton.tsx` referenced `React.MouseEvent` without importing the namespace) and was fixed in the same change. |

Lint runs on staged files in `.husky/pre-commit` (lint-staged) and full-repo via `npm run lint`. The visible total is now **0** across every rule, down from 149 at Pilot C start (and from the original 185 across all rules pre-enforcement). Because the baseline is empty, the former `SF_LINT_STAGED=1` downgrade — which suppressed `no-raw-button` + `canonical-loading-verb` during pre-commit so baseline noise on touched files wouldn't block commits — was removed 2026-06-22; both rules now fire at full severity in the gate, so a newly-added file carrying a raw `<button>` can no longer pass commit silently.

## What's NOT enforced

**Zero active clauses are unenforceable.** Every active clause in `docs/CORE.md` has a real enforcement mechanism: structural, test, or lint. As of audit triage close (2026-04-30), all Surface Contract clauses (#1–#5) are enforced — Surface #2, #3, #5 structurally; Surface #1 by lint+test; Surface #4 by test. **Process Contract #6 (Study throughline is structural) was added 2026-04-30 alongside the SFDI charter and activated 2026-05-04** when SFDI's four-sub-phase walk completed; enforcement is structural via the two live validator scripts (`scripts/sfdi-internal-consistency.py`, `scripts/sfdi-cross-doc-consistency.py` — both un-archived 2026-05-06 with the Field 3 unified-canvas rewrite). **SADI ratification 2026-05-04 extends Process #6 to all workspace steps through Delivery** (no separate clause); SADI's working doc becomes a second canonical-articulation pointer alongside SFDI's. The CORE.md text edit consummating this extension lands as a small downstream code-edit pass.

## What this document is for

When a future session asks "is clause X enforced?" — read this document, not memory.

When a contract test fails — the rejection cites the clause. Trace from clause number to this document's per-clause table to confirm the test is testing the right thing.

When a deferred clause's owning pilot lands — update this document. The deferred section is a working backlog; clauses move out of it as their owners complete.

When `docs/CORE.md` changes — re-verify this document. Clauses can be added, removed, or rewritten; this document must reflect the current contract.
