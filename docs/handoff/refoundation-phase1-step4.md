# Re-Foundation — next-session handoff (Phase 1, step 4)

> Paste the block below into a fresh session to continue. Written 2026-06-14 after
> Phase 1 steps 1–3 shipped to `main`.

```
Continue the Workspace Re-Foundation Initiative, Phase 1 — pick up at step 4.

Read these first, in order, before doing anything:
1. docs/PROPOSALS/refoundation-initiative.md — the working board: the whole plan,
   the locked decisions, per-step status (steps 1–3 marked DONE), the net-truth map
   ("Phase 1 · Step 1 output"), and the Merida appendix (the Phase-2 input). READ IT
   FIRST — the memory file is only a pointer.
2. memory project_refoundation_initiative — the progress log + per-step commit hashes.
3. docs/CORE.md (now slimmed), docs/WORKSPACE-CANON.md (the DRAFT canon),
   docs/CORE-CHANGELOG.md.

Where we are: Phase 1 steps 1–3 are DONE and pushed to main — net-truth map (two
ultracode fan-outs, 22-subject map), the verified WORKSPACE-CANON draft, and all three
contract examinations + the CORE slim. The pastor's examination-1 ruling is written in:
an honest "nothing here / doesn't apply" counts as done — N/A restored to the declared
SFDI Study questions + per thought-unit cell; the named-outcome paragraphs stay no-N/A.
IMPORTANT: the per-cell/Study N/A is a SCHEDULED CODE BUILD, not yet built — the docs
record the ruled target; the shipped code still enforces the two-question allowlist
(intro.redemptive_note + mps.gospel_check).

Next action — step 4: thin docs/SYSTEMS/sermon-workspace.md to *how & where*
(components, JSON columns, derivations, save flow), migrating the walk's *what & why*
up into WORKSPACE-CANON and cross-referencing it. Verify two things: (a) CORE now
points to sermon-workspace.md for the Pastoral Context mechanics — confirm it actually
holds them; (b) nothing gets dropped or orphaned. Solo + light verify per the board's
ultracode allocation.

Then step 5: banner-historicize the old initiatives (SFDI, SADI, SPRD, era-2,
invisible-system, workspace-restructure, saturation) as "authority transferred to
CORE + WORKSPACE-CANON — historical record," register WORKSPACE-CANON on
docs/ANCHORS.md, and lift its DRAFT banner — the moment the canon becomes the live
source. This is where the big un-propagated-doc cleanup lands (the SADI working doc,
the workspace-trail charter, the SPRD doc — see the net-truth map's cleanup worklists).

Also queued (independent of canon, do whenever): the separate infra-doc pass —
present-tense sql.js/saveDb cruft in ipc.md, sermon-workspace.md, ipc-channels.md,
project-structure.md, and the database.md schema-version v14-vs-v24 drift.

Constraints to hold: ground truth = the code at HEAD (code wins doc conflicts); report
before drafting or large edits; Phase 1 cuts no Merida question (that's Phase 2); the
canon stays DRAFT until step 5 ratifies it; bring diffs for approval before committing
anything that touches CORE.
```
