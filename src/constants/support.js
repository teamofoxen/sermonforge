// Mirrors electron/support.js — the support channel never changes at
// runtime, so a mirrored constant beats an IPC round-trip. Swap both
// files together when the dedicated forwarding address exists.

export const SUPPORT_EMAIL = "ross.appleton@gmail.com";
